# EECS581-Project-1
minesweepers project
Using typescript so need node installed
install Node from nodejs.org, accept the invite, clone with GitHub Desktop, then run npm install and npm run build in the project folder.
